// import { useAuth } from '../context/AuthContext';
import Login from './login';
import Home from './home';

const Main = () => {
  // const { token } = useAuth();
  return <Login />;
};

export default Main;
